<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mealTracker.mapper.MealSessionMapper">
    <select id="findActiveSessionId" resultType="long">
        SELECT id
        FROM meal_session
        WHERE ended_at IS NULL
        ORDER BY started_at DESC
        LIMIT 1
    </select>

    <insert id="insertSession" parameterType="com.example.mealTracker.dto.MealSession" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO meal_session(started_at, ended_at)
        VALUES (#{startedAt}, NULL)
    </insert>

    <update id="endSession">
        UPDATE meal_session
        SET ended_at = NOW()
        WHERE id = ${id}
    </update>

</mapper>